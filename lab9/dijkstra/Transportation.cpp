#include<iostream>
#include<vector>
using namespace std;
int main(){
    int n,m;cin>>n>>m;
    vector<int> max_w(m);
    vector<vector<pair<int,int>>>g(n);
    for(int i=0;i<m;i++){
        int a,b;
    }
    return 0;
}